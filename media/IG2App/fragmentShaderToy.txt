#version 330 core
in vec2 vUv0; // out del vertex shader

uniform sampler2D texture1;
out vec4 FragColor;

void main() {
vec3 color1 = vec3(texture(texture1,vUv1));
if(color1.r > 0.5)
discard;

gl_FragColor = color1;

}